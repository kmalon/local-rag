spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ragdb
    username: raguser
    password: ragpass
  ai:
    vectorstore:
      pgvector:
        initialize-schema: true
        dimensions: 384
